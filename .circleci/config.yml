version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout 

     # build image
     - run: |         
         docker info
         docker build -t aspnetapp -f Dockerfile .
     # deploy the image
     - run: |         
         docker login --username=$ultwolf@gmail.com --password-stdin registry.heroku.com
         docker tag aspnetapp registry.heroku.com/costsanalyse/web
         docker push registry.heroku.com/costsanalyse/web                
         curl https://cli-assets.heroku.com/install.sh | sh
         heroku container:release web -a costsanalyse